import { Typography } from "@mui/material";
import { WhiteBackgroundColor } from "~/utils/Theme";

const WebDevelopmentDescription = (): JSX.Element => {
    return (
        <Typography variant="h4" style={{ color: WhiteBackgroundColor }}>
            Proficient in building dynamic and responsive web applications using the React framework. Skilled in creating reusable components, managing state with Redux or Context API, and integrating RESTful APIs. Experienced in
            modern JavaScript (ES6+), HTML, and CSS, with a strong understanding of front-end best practices and performance optimization. Capable of translating design wireframes into high-quality code and collaborating
            effectively within a team environment.
        </Typography>
    );
};

export default WebDevelopmentDescription;
